{
  "name": "mern-task-manager",
  "version": "1.0.0",
  "description": "Full-stack MERN Task Manager with authentication, protected routes, and complete CRUD operations.",
  "main": "index.js",
  "author": "Tanmay Awasthi",
  "license": "MIT",
  "keywords": [
    "mern",
    "react",
    "nodejs",
    "express",
    "mongodb",
    "task manager",
    "full stack application",
    "mern project",
    "todo app",
    "jwt authentication"
  ],
  "scripts": {
    "dev-server": "npm run dev --prefix backend",
    "dev-client": "npm start --prefix frontend",
    "dev": "concurrently \"npm run dev-server\" \"npm run dev-client\"",

    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",

    "build": "npm install --prefix frontend && npm run build --prefix frontend",

    "start": "cd backend && node app.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
